import random
objects = "PRO-1234"

def genrete():
    while True:
        random_number = random.randint(10000, 99999)  # 5-digit random number
        order_number = f"PRO-{random_number}"
        if not objects ==  "PRO-1234": 
            return genrete()
        else:
            return order_number
        

val = genrete()
print(val)